rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, allow read/write access to all documents for authenticated users,
    // or anyone during development if auth is not fully configured yet.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
